A web implementation of the card game SET.

A **set** is a group of three cards where every attribute
(colour, shape, shading, count) is either *all the same* or *all
different* across the three cards.

More information is given in the game’s instructions.